#ifndef DERIVATIVE_H
#define DERIVATIVE_H

#include <cmath>

class derivative{
	private:
		// Vector to hold exponents x^2, y, etc...
		// Vector to hold elements 2, 4, 6
		// String to hold the user input
	public:
		// Function to populate vectors based on input string
		// Function to calculate derivate of input string 
};

#endif
